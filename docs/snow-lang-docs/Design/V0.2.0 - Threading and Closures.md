# Goals
- [ ] Basic Types
	- [ ] Task
- [ ] Lifetime safety rule 3
- [ ] `async` keyword for functions
- [ ] Function type accepts `async`
- [ ] `await` keyword
- [ ] Async functions
	- [ ] `yield` keyword
		- [ ] `yield var_value;` returns said var or value from the function
- [ ] Closures
- [ ] Threads
- [ ] Promises
- [ ] Class
	- [ ] Mutable static variables which are atomically secure are now allowed
- [ ] `mutex`, `semaphore` and `spinlock`
	- [ ] [linux - When should we use mutex and when should we use semaphore - Stack Overflow](https://stackoverflow.com/questions/4039899/when-should-we-use-mutex-and-when-should-we-use-semaphore)
- [ ] `ashared<T>`
	- The same as a `shared<T>` but it is atomically secured. This will allow you to share on threads
- [ ] `atomic<T>`
	- Atomically secures a type allowing only one thread to gain access to it at a time
- [ ] 

# Non-Goals

# Details and Design
## A NOTE:
- Nothing here is final, this is the bleeding edge of the language design
- Task probably shouldnt be type, it should probably just be a function
## Task
- Wrapper around a function which utilizes threading mechanics, similar to coroutines
- Function wrapped must be `async`
- `task(F)` where F is an async function type for typename
- `let t : task(fn()) = someFunc;`
- Methods
	- `fn state(): TaskState`: Returns the task's state
		- `TaskState` is an enum. `NotStarted`, `Running`, `Paused = typeof(F).yeildType`, `Finished = typeof(F).returnType`
	- `fn await(): typeof(F).returnType`: Blocks the current thread awaiting for the task to complete, yields are immediately resolves, their value is discarded, returns the functions return type when finished
	- `fn isPaused(): bool`: Returns true if paused
	- `fn isFinished(): bool`: Returns true if finished
	- `fn isStarted(): bool`: Returns true if the task has been executed at least once (can be true if finished)
	- `fn isRunning(): bool`: Returns true if the task is currently executing (on a different thread of course)
	- `fn execute(): enum { typeof(F).yeildType, typeof(F).returnType }`: Executes the task until a yield or return is called
## Lifetime Safety
- Rule 3:
	3) Variables must be moved, copied, or atomically secured to be used in threads or closures. (Not implemented this release, v0.2.0)
		```
		fn func(a : ref i32) {}
		
		// This syntax is not final for threads
		let t;
		{
			let x = 4;
			t = Thread(func, ref x);
		} // x goes out of scope, thread is not. Thread's reference is now invalid memory
		```
		- Threads and closures work similar to one another in that they can reference variables that exist outside of their own internal scope. This can result in invalid memory, so variables must be copied, moved, or atomically secured to be provided to a thread or closure.
		- Atomically securing is done through a class attribute or by wrapping an object in an atomically secured class such as an `asref` (atomic shared reference)
		- There is one caveat to this rule: Closures who do are not referenced outside the scope they are defined in, can capture references as the scope for the closure is well defined that it will not leave
## Closures
- `let x = 4; let closure = fn[copy x]() {};`
- Closures are basically a function mixed with a tuple. The `[]` section after `fn` but before the the parameter list is the capture list. The capture list provides arguments to the closure that are not in the parameter list
	```
	fn[
		copy a, // an immutable value 'a' will be available, copied from the value 'a' outside the closure
		mut move y, // similar to a, but b will be moved and 'b' is mutable in the closure
		ref z, // an immutable reference z will be available
		mut ref w, // a mutable reference w will be available
		newName = mut ref oldName, // newName is a mutable reference, it references oldName
		n2 = mut o2.getThing() // Similar to newName, but here `o2.getThing()` will be called before the closure is made
	](x : u32, y : u32) {}
	```
	- The capture list is similar to an assignment of a variable, so `mut ref` is to assign a mutable reference. `mut` itself is not allowed. However, `mut move` or `mut copy` are for value based
- Closures that capture arguments must follow threading lifetime safety rules
- Closures that do not capture arguments are treated no differently than any other global function
## Threads
- `mutex`, `semaphore`, `spinlock` might be good to add to as std library objects
- 